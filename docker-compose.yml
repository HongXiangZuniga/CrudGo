version: "3.3"
services:
  mongo:
    image: "mongo"
    ports: 
      - 27017:27017
    networks:
      testing_net:
        ipv4_address: 172.25.0.4

networks:
    testing_net:
        ipam:
            driver: default
            config:
                - subnet: 172.25.0.0/16